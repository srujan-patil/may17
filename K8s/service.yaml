apiVersion: v1
kind: Service
metadata:
  name: srujan-portfolio-service
spec:
  selector:
    app: srujan-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer

